<h1>Tasks list</h1>

<ul>
	<% @tasks.each do |task| %>
<li><%= link_to task.title, task_path(task) %> <%= link_to "Delete", task, method: :delete, data: { confirm: "Are you sure?" } %></li>
<% end %>
</ul>

<%= link_to "Add a task", new_task_path %>